# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Book {
  author: String!
  createdAt: DateTime!
  id: ID!
  title: String!
}

input BookWhereInput {
  author: String
  title: String
}

input CreateBookInput {
  author: String!
  title: String!
}

input CreateReservationInput {
  bookId: String!
  reservationDate: DateTime!
  returnDate: DateTime!
  userId: String!
}

input CreateUserInput {
  email: String!
  name: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  createBook(data: CreateBookInput!): Book!
  createReservation(data: CreateReservationInput!): Reservation!
  createUser(data: CreateUserInput!): User!
  deleteBook(id: String!): Book!
  returnBook(reservationId: String!): Reservation!
  updateBook(data: UpdateBookInput!): Book!
}

type PaginatedBooks {
  data: [Book!]!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type PaginatedUsers {
  data: [User!]!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type Query {
  allReservations(filter: ReservationFilterInput): [Reservation!]!
  availableBooks: [Book!]!
  book(id: String!): Book
  findAllBooks(limit: Int = 10, page: Int = 1, where: BookWhereInput): PaginatedBooks!
  findAllUsers(limit: Int = 10, page: Int = 1): PaginatedUsers!
  reservationsByBook(bookId: String!, filter: ReservationFilterInput): [Reservation!]!
  reservationsByUser(filter: ReservationFilterInput, userId: String!): [Reservation!]!
  user(id: String!): User
}

type Reservation {
  book: Book!
  bookId: String!
  createdAt: DateTime!
  daysLeft: Int!
  id: ID!
  isLate: Boolean!
  lateDays: Int!
  reservationDate: DateTime!
  returnDate: DateTime!
  returned: Boolean!
  returnedAt: DateTime!
  user: User!
  userId: String!
}

input ReservationFilterInput {
  endDate: DateTime
  startDate: DateTime
}

input UpdateBookInput {
  author: String
  id: ID!
  title: String
}

type User {
  activeReservations: Float!
  createdAt: DateTime!
  email: String!
  id: ID!
  isBanned: Boolean!
  name: String!
}